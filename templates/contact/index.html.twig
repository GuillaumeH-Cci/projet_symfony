{% extends 'base.html.twig' %}

{% block title %}Contact{% endblock %}

{% block body %}
    <div class="row justify-content-center">
        <div class="col-lg-6">
            <div class="card shadow-sm mb-4">
                <div class="card-body">
                    <h1 class="card-title h4 mb-4">Contact</h1>

                    {% for label, messages in app.flashes %}
                        <div class="alert alert-{{ label }}">
                            {% for message in messages %}
                                <div>{{ message }}</div>
                            {% endfor %}
                        </div>
                    {% endfor %}

                    {{ form_start(contactForm, {'attr': {'class': 'needs-validation'}}) }}
                        <div class="mb-3">
                            {{ form_label(contactForm.name) }} {# Affiche le label du champ "name" #}
                            {{ form_widget(contactForm.name, {'attr': {'class': 'form-control'}}) }} {# Affiche le widget du champ "name" avec la classe CSS "form-control" #}
                            {{ form_errors(contactForm.name) }} {# Affiche les erreurs de validation pour le champ "name" #}
                        </div>
                        <div class="mb-3">
                            {{ form_label(contactForm.email) }}
                            {{ form_widget(contactForm.email, {'attr': {'class': 'form-control'}}) }}
                            {{ form_errors(contactForm.email) }}
                        </div>
                        <div class="mb-3">
                            {{ form_label(contactForm.message) }}
                            {{ form_widget(contactForm.message, {'attr': {'class': 'form-control'}}) }}
                            {{ form_errors(contactForm.message) }}
                        </div>
                        <div class="d-grid">
                            {{ form_widget(contactForm.submit) }}
                        </div>
                    {{ form_end(contactForm) }}
                </div>
            </div>
        </div>
    </div>
{% endblock %}
